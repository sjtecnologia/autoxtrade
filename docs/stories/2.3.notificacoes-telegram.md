# Story 2.3: Notificações Telegram

## Status

Draft

## Story

**As a** owner do autoxtrade,
**I want** receber notificações via Telegram a cada evento importante do bot,
**so that** eu esteja informado sobre operações e alertas sem precisar abrir o dashboard.

## Acceptance Criteria

1. Bot Telegram configurado (token via variável de ambiente); `sendMessage` funcional
2. Notificação enviada ao abrir posição: símbolo, lado (LONG/SHORT), preço entrada, quantidade, stop loss, take profit
3. Notificação enviada ao fechar posição: símbolo, lado, resultado em R$ e %, duração, motivo (TP hit / SL hit / manual / drawdown limit)
4. Notificação enviada em caso de erro crítico da API da exchange (conexão perdida, ordem rejeitada, OCO_FAILED)
5. Notificação com relatório diário às 23h BRT: operações do dia, P&L do dia, P&L acumulado
6. Fila de notificações com retry: se envio falhar, tenta novamente até 3 vezes com backoff exponencial
7. Testes unitários para formatação das mensagens Telegram

## Tasks / Subtasks

- [ ] Criar wrapper do Telegram Bot API (AC: 1)
  - [ ] `backend/notifications/telegram.py`: classe `TelegramNotifier`
  - [ ] Usar `python-telegram-bot` (async): `Bot(token=settings.TELEGRAM_BOT_TOKEN)`
  - [ ] Método `send_message(text, parse_mode="HTML")`: enviar para `settings.TELEGRAM_CHAT_ID`
  - [ ] Método `send_emergency_alert(text)`: wrapper com prefixo `🚨 EMERGÊNCIA:`
  - [ ] Configurar instância singleton acessível em todo o backend
- [ ] Criar templates de mensagens (AC: 2, 3, 4, 5)
  - [ ] `backend/notifications/templates.py`: funções de formatação
  - [ ] `format_position_opened(trade)`: 
    ```
    📈 POSIÇÃO ABERTA
    <b>BTC/USDT LONG</b>
    Entrada: R$ 287.450,00
    Qtd: 0,001
    Stop Loss: R$ 282.900,00 (-1,58%)
    Take Profit: R$ 294.000,00 (+2,28%)
    Capital em risco: R$ 65,00 (1%)
    ```
  - [ ] `format_position_closed(trade)`:
    ```
    ✅ POSIÇÃO FECHADA — TP HIT
    <b>BTC/USDT LONG</b>
    Resultado: +R$ 65,50 (+2,28%)
    Duração: 4h 23min
    Modelo: rf_v20260428 (conf: 78%)
    ```
  - [ ] `format_drawdown_alert(drawdown_pct, status)`: aviso ou emergência por threshold
  - [ ] `format_oco_failed(trade, error)`: alerta de emergência OCO
  - [ ] `format_daily_report(stats)`: relatório diário completo
- [ ] Implementar fila com retry via Celery (AC: 6)
  - [ ] `backend/tasks/report_tasks.py`: tarefa Celery `send_telegram_message.apply_async()`
  - [ ] Configurar retry: `max_retries=3`, `countdown=2` (2s, 4s, 8s — backoff)
  - [ ] Se todas as tentativas falharem: logar erro crítico (não lançar exceção que possa afetar o trading)
- [ ] Configurar relatório diário agendado (AC: 5)
  - [ ] `backend/tasks/report_tasks.py`: tarefa `send_daily_report()`
  - [ ] Buscar stats do dia: trades, P&L do dia, P&L acumulado, Win Rate do dia, drawdown atual
  - [ ] Agendar via Celery Beat: `crontab(hour=23, minute=0, timezone="America/Sao_Paulo")`
- [ ] Integrar notificações nos eventos do sistema (AC: 2, 3, 4)
  - [ ] Em `order_executor.py` → chamar `telegram.send_message(format_position_opened(trade))` após abertura
  - [ ] Em `order_monitor.py` → chamar `telegram.send_message(format_position_closed(trade))` após fechamento
  - [ ] Em `drawdown_monitor.py` → chamar notificação ao cruzar 10% e 15%
  - [ ] Em `order_executor.py` fallback OCO → chamar `telegram.send_emergency_alert(format_oco_failed(...))`
- [ ] Escrever testes unitários (AC: 7)
  - [ ] `backend/tests/unit/test_telegram.py`
  - [ ] Testar cada função de formatação com dados conhecidos
  - [ ] Verificar que textos contêm campos obrigatórios (símbolo, valor, percentual)
  - [ ] Testar com valores negativos (P&L negativo)
  - [ ] Testar formatação de relatório diário sem operações (dia sem trades)

## Dev Notes

**python-telegram-bot async:**
```python
from telegram import Bot

class TelegramNotifier:
    def __init__(self):
        self._bot = Bot(token=settings.TELEGRAM_BOT_TOKEN)

    async def send_message(self, text: str, parse_mode: str = "HTML") -> bool:
        try:
            await self._bot.send_message(
                chat_id=settings.TELEGRAM_CHAT_ID,
                text=text,
                parse_mode=parse_mode
            )
            return True
        except Exception as e:
            log.error("telegram_send_failed", error=str(e))
            return False
```

**Retry via Celery (não bloquear o loop de trading):**
```python
# backend/tasks/report_tasks.py
from celery import shared_task

@shared_task(bind=True, max_retries=3, default_retry_delay=2)
def send_telegram_async(self, text: str, parse_mode: str = "HTML"):
    try:
        # criar novo event loop para tarefa síncrona Celery
        asyncio.run(notifier.send_message(text, parse_mode))
    except Exception as exc:
        raise self.retry(exc=exc, countdown=2 ** self.request.retries)
```

**Celery Beat schedule para relatório diário:**
```python
CELERYBEAT_SCHEDULE = {
    "daily-report": {
        "task": "tasks.report_tasks.send_daily_report",
        "schedule": crontab(hour=23, minute=0, timezone="America/Sao_Paulo"),
    },
    "update-market-data": {
        "task": "tasks.data_tasks.update_market_data",
        "schedule": crontab(hour=3, minute=0),  # 03h BRT
    },
}
```

**Formatação de valores monetários (BRL):**
```python
from babel.numbers import format_currency

def fmt_brl(value: float) -> str:
    return format_currency(value, "BRL", locale="pt_BR")  # R$ 1.234,56
```
Adicionar `babel` ao `requirements.txt`.

**Mensagem de emergência deve:**
- Não depender de banco de dados (banco pode estar down no momento do emergência)
- Passar texto diretamente, sem formatação complexa
- Ter timeout curto (5s) para não travar o fluxo

**Localização dos arquivos:**
```
backend/notifications/telegram.py
backend/notifications/templates.py
backend/tasks/report_tasks.py
backend/tests/unit/test_telegram.py
```

### Testing

**Framework:** pytest + unittest.mock

**Localização:** `backend/tests/unit/test_telegram.py`

**Cobertura mínima:** 60% em `backend/notifications/` (conforme arquitetura seção 10.2)

**Padrão de testes (formatação — sem chamadas reais ao Telegram):**
```python
def test_format_position_opened():
    from notifications.templates import format_position_opened
    from decimal import Decimal

    trade = Trade(symbol="BTC/USDT", side="LONG", entry_price=Decimal("287450"),
                  quantity=Decimal("0.001"), stop_loss=Decimal("282900"),
                  take_profit=Decimal("294000"), risk_amount=Decimal("65"))

    msg = format_position_opened(trade)
    assert "BTC/USDT" in msg
    assert "LONG" in msg
    assert "287.450" in msg
    assert "Stop Loss" in msg

def test_format_position_closed_loss():
    # Testar com P&L negativo para garantir formatação correta
    ...
```

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 28/04/2026 | 1.0 | Criação inicial | SM (BMad) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
