# Story 1.2: Containerização com Docker Compose

## Status

Draft

## Story

**As a** owner do autoxtrade,
**I want** todos os serviços (backend, frontend, PostgreSQL, Redis) containerizados e orquestrados via Docker Compose,
**so that** o ambiente seja reproduzível e o deploy seja simples com um único comando.

## Acceptance Criteria

1. `docker-compose.yml` define serviços: `backend`, `frontend`, `db` (PostgreSQL), `redis`
2. `Dockerfile` para backend Python e frontend Next.js criados e funcionais
3. Variáveis de ambiente carregadas de arquivo `.env` (não versionado); `.env.example` documentado no repositório
4. Comando `docker compose up -d` levanta todos os serviços sem erros
5. Healthcheck configurado para cada serviço; `docker compose ps` mostra todos `healthy`
6. Volumes persistentes configurados para PostgreSQL e Redis

## Tasks / Subtasks

- [ ] Criar Dockerfile do backend (AC: 2)
  - [ ] Base image: `python:3.11-slim`
  - [ ] Instalar dependências do sistema necessárias para TA-Lib: `build-essential`, `libta-lib-dev`
  - [ ] Copiar `requirements.txt` e executar `pip install --no-cache-dir -r requirements.txt`
  - [ ] Copiar código do backend
  - [ ] Definir `CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]`
  - [ ] Adicionar `HEALTHCHECK CMD curl -f http://localhost:8000/health || exit 1`
- [ ] Criar Dockerfile do frontend (AC: 2)
  - [ ] Build stage: `node:20-alpine` — instalar deps + `next build`
  - [ ] Runtime stage: `node:20-alpine` — copiar `.next/standalone` para imagem mínima
  - [ ] Expor porta 3000
  - [ ] Definir `CMD ["node", "server.js"]`
- [ ] Criar docker-compose.yml principal (AC: 1, 4, 5, 6)
  - [ ] Serviço `backend`: build `./backend`, porta 8000 (interna apenas), depends_on postgres+redis, healthcheck HTTP `/health`, `restart: unless-stopped`
  - [ ] Serviço `frontend`: build `./frontend`, porta 3000 (interna apenas), depends_on backend, `restart: unless-stopped`
  - [ ] Serviço `db` (PostgreSQL 16): image `postgres:16-alpine`, volume `pg_data:/var/lib/postgresql/data`, env vars `POSTGRES_*`, healthcheck `pg_isready`, `restart: unless-stopped`
  - [ ] Serviço `redis` (Redis 7): image `redis:7-alpine`, volume `redis_data:/data`, healthcheck `redis-cli ping`, `restart: unless-stopped`
  - [ ] Serviço `celery_worker`: mesmo build do backend, command `celery -A tasks.celery_app worker --loglevel=info`, depends_on postgres+redis
  - [ ] Serviço `celery_beat`: mesmo build, command `celery -A tasks.celery_app beat --loglevel=info`
  - [ ] Serviço `nginx`: image `nginx:alpine`, volumes conf + certbot, portas 80 e 443, depends_on frontend+backend
  - [ ] Network `autoxtrade_net` definida; todos os serviços na mesma rede
  - [ ] Volumes nomeados: `pg_data`, `redis_data`, `nginx_certs`
- [ ] Criar docker-compose.override.yml para desenvolvimento local (AC: 3)
  - [ ] Backend: montar volume `./backend:/app` para hot reload; command com `--reload`
  - [ ] Frontend: montar volume `./frontend:/app` para hot reload; command `npm run dev`
  - [ ] Expor porta 8000 e 3000 diretamente no host para desenvolvimento
- [ ] Criar .env.example completo (AC: 3)
  - [ ] Todas as variáveis do sistema documentadas com comentários
  - [ ] Valores default para desenvolvimento (ex: `BINANCE_PAPER_MODE=true`)
  - [ ] Incluir: `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `REDIS_URL`, `DATABASE_URL`, `SECRET_TOKEN`, `BINANCE_*`, `TELEGRAM_*`, `MT5_*`
- [ ] Criar scripts/setup_vps.sh (AC: 4 — facilita deploy no VPS)
  - [ ] Instalar Docker e Docker Compose no Ubuntu 22.04
  - [ ] Configurar UFW firewall: permitir apenas 22 (SSH), 80 (HTTP), 443 (HTTPS)
  - [ ] Instalar certbot para HTTPS
  - [ ] Criar diretório `/opt/autoxtrade/` com permissões corretas
- [ ] Validar: `docker compose up -d` e `docker compose ps` (AC: 4, 5)
  - [ ] Confirmar todos os serviços `healthy` antes de dar story como completa

## Dev Notes

**docker-compose.yml de produção completo** está em `docs/fullstack-architecture.md` seção 6.2 — usar como referência principal.

**Serviços e portas internas:**
```
nginx        → :80, :443 (externo)
frontend     → :3000 (apenas interno)
backend      → :8000 (apenas interno)
celery_worker → sem porta
celery_beat  → sem porta
postgres     → :5432 (apenas interno)
redis        → :6379 (apenas interno)
```

**Healthchecks:**
```yaml
# backend
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 40s

# postgres
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
  interval: 10s
  timeout: 5s
  retries: 5

# redis
healthcheck:
  test: ["CMD", "redis-cli", "ping"]
  interval: 10s
  timeout: 3s
  retries: 3
```

**Volumes críticos:**
- `pg_data` → dados PostgreSQL (NÃO apagar sem backup)
- `redis_data` → dados Redis (filas Celery — menor criticidade)

**Segurança:** PostgreSQL e Redis não devem expor portas para o host em produção. Apenas nginx expõe portas externas (80/443). Backend e frontend acessíveis apenas via nginx.

**TA-Lib:** A biblioteca TA-Lib requer compilação a partir do código C. O Dockerfile do backend precisa instalar `libta-lib-dev` antes do pip install. Alternativa: usar `ta-lib-python` wheels pré-compilados.

**Referências:**
- `docs/fullstack-architecture.md` seção 6.2 (docker-compose.yml completo)
- `docs/fullstack-architecture.md` seção 6.3 (nginx config)
- `docs/fullstack-architecture.md` seção 6.5 (setup_vps.sh)

### Testing

**Framework:** verificação manual + Docker
- `docker compose up -d` → sem erros no stdout
- `docker compose ps` → todos com status `healthy` (pode levar até 60s para start_period)
- `docker compose logs backend` → verificar ausência de erros de importação
- `curl http://localhost:8000/health` → `{"status": "ok"}` (com `docker-compose.override.yml` ativo)

Não há testes automatizados para a containerização — validação é por execução real.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 28/04/2026 | 1.0 | Criação inicial | SM (BMad) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
