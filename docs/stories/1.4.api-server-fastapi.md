# Story 1.4: API Server Base (FastAPI) + WebSocket

## Status

Draft

## Story

**As a** dashboard frontend,
**I want** uma API REST funcional com suporte a WebSocket,
**so that** o frontend possa buscar dados e receber atualizações em tempo real.

## Acceptance Criteria

1. FastAPI inicializado com estrutura de routers modulares (`/api/v1/`)
2. Endpoint `GET /health` retorna status `{"status": "ok", "timestamp": "..."}` com HTTP 200
3. Endpoint `GET /api/v1/config` retorna configuração atual do bot
4. Endpoint `POST /api/v1/config` atualiza configuração (bot ativo/pausado por mercado)
5. WebSocket endpoint `WS /ws` estabelece conexão e envia `ping` a cada 30 segundos
6. Autenticação básica via token estático configurado em variável de ambiente (sem multi-usuário)
7. Testes unitários para todos os endpoints com pytest — cobertura ≥ 80%

## Tasks / Subtasks

- [ ] Criar entrypoint FastAPI (AC: 1)
  - [ ] `backend/main.py`: criar app FastAPI com título, versão e descrição
  - [ ] Configurar CORS: permitir origem do frontend (configurável via env `ALLOWED_ORIGINS`)
  - [ ] Incluir router principal: `app.include_router(api_router, prefix="/api/v1")`
  - [ ] Registrar handler de startup: inicializar pool de conexões DB, Redis
  - [ ] Registrar handler de shutdown: fechar conexões
- [ ] Criar sistema de configurações (AC: 1)
  - [ ] `backend/config.py`: classe `Settings` com pydantic-settings lendo `.env`
  - [ ] Campos: `DATABASE_URL`, `REDIS_URL`, `SECRET_TOKEN`, `BINANCE_API_KEY`, `BINANCE_API_SECRET`, `BINANCE_PAPER_MODE`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ALLOWED_ORIGINS`, `LOG_LEVEL`
  - [ ] Instância singleton: `settings = Settings()`
- [ ] Criar middleware de autenticação (AC: 6)
  - [ ] `backend/api/auth.py`: função `verify_token(credentials: HTTPAuthorizationCredentials)`
  - [ ] Compara `credentials.credentials` com `settings.SECRET_TOKEN` usando `secrets.compare_digest` (timing-safe)
  - [ ] Retorna HTTP 401 com mensagem genérica se token inválido
  - [ ] Rota `/health` é pública (sem autenticação)
- [ ] Criar endpoint de health (AC: 2)
  - [ ] `backend/api/routes/health.py`: `GET /health`
  - [ ] Verificar conectividade com PostgreSQL (`SELECT 1`)
  - [ ] Verificar conectividade com Redis (`PING`)
  - [ ] Resposta: `{"status": "ok"|"degraded", "timestamp": "ISO8601", "services": {"db": "ok", "redis": "ok"}}`
- [ ] Criar endpoints de configuração (AC: 3, 4)
  - [ ] `backend/api/routes/config.py`
  - [ ] `GET /api/v1/config`: retorna lista de `BotConfig` de todos os mercados
  - [ ] `PATCH /api/v1/config/{market}`: atualiza campos específicos do `BotConfig`
  - [ ] Validação via Pydantic schema `BotConfigUpdateSchema`
  - [ ] Campos atualizáveis: `is_active`, `mode`, `risk_per_trade_pct`, `max_open_trades`, `max_drawdown_pct`
  - [ ] Registrar alterações em `config_audit_log` automaticamente
- [ ] Criar endpoint WebSocket (AC: 5)
  - [ ] `backend/api/websocket.py`: `WS /ws`
  - [ ] Autenticar via query param `?token=<SECRET_TOKEN>` na handshake
  - [ ] Manter lista de conexões ativas (`connection_manager`)
  - [ ] Enviar `{"type": "PING", "ts": "..."}` a cada 30 segundos via asyncio task
  - [ ] Broadcast `{"type": "PRICE_UPDATE", ...}` quando receber dados de mercado via Redis pub/sub
  - [ ] Tratar desconexões: remover da lista sem erro
- [ ] Criar schemas Pydantic (AC: 3, 4)
  - [ ] `backend/api/schemas/config.py`: `BotConfigSchema`, `BotConfigUpdateSchema`
  - [ ] `backend/api/schemas/health.py`: `HealthResponseSchema`
- [ ] Escrever testes unitários (AC: 7)
  - [ ] `backend/tests/integration/test_api_endpoints.py`
  - [ ] Testar `GET /health` → 200 com corpo correto
  - [ ] Testar `GET /health` sem token → 200 (rota pública)
  - [ ] Testar `GET /api/v1/config` sem token → 401
  - [ ] Testar `GET /api/v1/config` com token válido → 200
  - [ ] Testar `PATCH /api/v1/config/CRIPTO` com dados válidos → 200
  - [ ] Testar `PATCH /api/v1/config/CRIPTO` com risco acima de 5% → 422 (validação)
  - [ ] Usar `httpx.AsyncClient(app=app, base_url="http://test")` para testes sem servidor

## Dev Notes

**Autenticação:**
```python
# backend/api/auth.py
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
import secrets

security = HTTPBearer()

async def verify_token(credentials: HTTPAuthorizationCredentials = Depends(security)):
    if not secrets.compare_digest(credentials.credentials, settings.SECRET_TOKEN):
        raise HTTPException(status_code=401, detail="Invalid authentication credentials")
    return credentials.credentials
```
⚠️ Usar `secrets.compare_digest` — previne timing attacks (OWASP A07).

**Estrutura de routers:**
```python
# backend/api/router.py
from fastapi import APIRouter
from api.routes import health, config, positions, trades, metrics, ml, backtest, risk

api_router = APIRouter()
api_router.include_router(config.router, prefix="/config", tags=["config"])
# ... demais routers adicionados nas stories seguintes
```

**WebSocket — Gerenciador de conexões:**
```python
# backend/api/websocket.py
class ConnectionManager:
    def __init__(self):
        self.active_connections: list[WebSocket] = []

    async def connect(self, websocket: WebSocket):
        await websocket.accept()
        self.active_connections.append(websocket)

    async def broadcast(self, message: dict):
        for connection in self.active_connections:
            try:
                await connection.send_json(message)
            except WebSocketDisconnect:
                self.active_connections.remove(connection)
```

**Eventos WebSocket definidos na arquitetura:**
- `PRICE_UPDATE`, `POSITION_UPDATE`, `POSITION_OPENED`, `POSITION_CLOSED`
- `DRAWDOWN_ALERT`, `BOT_STATUS_CHANGED`, `MODEL_READY`, `PING`
- Contrato completo: `docs/fullstack-architecture.md` seção 3.3

**Endpoints REST completos** (todos os 25+): `docs/fullstack-architecture.md` seção 3.2 — apenas config e health são implementados nesta story; os demais serão adicionados nas stories de cada epic.

**Logging estruturado:**
```python
import structlog
log = structlog.get_logger()
log.info("config_updated", market="CRIPTO", field="is_active", old=False, new=True)
```

### Testing

**Framework:** pytest + pytest-asyncio + httpx

**Localização dos testes:** `backend/tests/integration/test_api_endpoints.py`

**Cobertura mínima:** ≥ 80% em `backend/api/routes/` (conforme arquitetura seção 10.2)

**Padrão de testes:**
```python
import pytest
from httpx import AsyncClient, ASGITransport
from main import app

@pytest.fixture
async def client():
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        yield c

@pytest.mark.asyncio
async def test_health_check(client):
    response = await client.get("/health")
    assert response.status_code == 200
    assert response.json()["status"] in ("ok", "degraded")

@pytest.mark.asyncio
async def test_config_requires_auth(client):
    response = await client.get("/api/v1/config")
    assert response.status_code == 401
```

**Fixtures:** `backend/tests/conftest.py` deve prover `db_session` com banco de teste e override da dependency `get_db`.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 28/04/2026 | 1.0 | Criação inicial | SM (BMad) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
