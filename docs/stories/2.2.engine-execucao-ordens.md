# Story 2.2: Engine de Execução de Ordens

## Status

Draft

## Story

**As a** engine de trading,
**I want** enviar ordens de compra e venda com stop loss obrigatório diretamente na exchange,
**so that** cada posição aberta tenha proteção de risco garantida mesmo em caso de falha do servidor.

## Acceptance Criteria

1. Suporte a ordens: market, limit e OCO (stop loss + take profit simultâneos)
2. Toda ordem de compra/abertura de posição DEVE ser acompanhada de ordem OCO de stop loss na exchange (server-side) — sistema rejeita abertura sem stop loss configurado
3. Sistema persiste todas as ordens enviadas na tabela `trades` com status `open`
4. Sistema monitora status das ordens via WebSocket e atualiza `trades` quando ordem é executada ou cancelada
5. Em modo paper trading, ordens são simuladas: stop loss e take profit disparam quando preço cruza os níveis simulados
6. Endpoint `GET /api/v1/positions` retorna posições abertas com dados em tempo real
7. Testes unitários para lógica de construção de ordens OCO com 100% de cobertura
8. **Fallback de falha OCO:** Se o envio da ordem OCO falhar, o sistema deve: (a) tentar enviar stop-market separado como fallback; (b) se falhar, fechar posição a mercado imediatamente; (c) registrar evento `OCO_FAILED` em `trade_events`; (d) enviar notificação Telegram de emergência
9. **Monitoramento de posições órfãs:** Na inicialização, verificar posições abertas na exchange sem OCO correspondente — recriar OCO ou fechar a mercado

## Tasks / Subtasks

- [ ] Criar OrderExecutor (AC: 1, 2, 3)
  - [ ] `backend/trading/order_executor.py`: classe `OrderExecutor`
  - [ ] Método `open_position(symbol, side, quantity, entry_price, stop_loss, take_profit)`:
    - Validar que `stop_loss` está definido (rejeitar com exceção se não)
    - Validar que stop_loss está no lado correto (LONG: stop < entry; SHORT: stop > entry)
    - Enviar ordem de entrada (market ou limit) via connector
    - Imediatamente após confirmação: enviar ordem OCO (stop_loss + take_profit)
    - Persistir trade no banco com status `open` e ids das ordens
    - Registrar evento `ORDER_SENT` em `trade_events`
  - [ ] Método `close_position(trade_id, reason)`: cancelar OCO, enviar ordem market de fechamento, atualizar trade no banco
  - [ ] Método `_build_oco_order(symbol, side, quantity, stop_price, take_profit_price)`: retornar payload formatado para Binance OCO API
- [ ] Implementar fallback de falha OCO (AC: 8)
  - [ ] Ao receber exceção no envio do OCO:
    - Tentar `place_stop_market_order(symbol, stop_price)` como fallback
    - Se falhar: chamar `close_position_at_market(trade_id)` imediatamente
    - Registrar `TradeEvent(event_type="OCO_FAILED", payload={"error": str(e), "action": "market_close"})` 
    - Enviar notificação Telegram de emergência via `telegram.send_emergency_alert()`
- [ ] Implementar verificação de órfãos na inicialização (AC: 9)
  - [ ] `backend/trading/order_monitor.py`: método `check_orphan_positions()`
  - [ ] Buscar trades com `status="open"` no banco
  - [ ] Para cada trade: verificar se OCO correspondente existe na Binance via `get_open_orders(symbol)`
  - [ ] Se OCO ausente: tentar recriar OCO com stop_loss e take_profit do trade
  - [ ] Se recriar falhar: chamar `close_position_at_market(trade_id)` e notificar
  - [ ] Chamar no startup do backend (evento `lifespan` do FastAPI)
- [ ] Implementar monitor de ordens (AC: 4)
  - [ ] `backend/trading/order_monitor.py`: método `monitor_orders()`
  - [ ] Assinar stream de execução de ordens da Binance via CCXT WebSocket
  - [ ] Quando OCO executar (TP ou SL): atualizar trade no banco com `status="closed"`, `exit_price`, `close_reason`, calcular `pnl_net`
  - [ ] Registrar evento `TP_HIT` ou `SL_HIT` em `trade_events`
  - [ ] Publicar evento via Redis pub/sub para broadcast ao frontend via WebSocket
- [ ] Criar endpoint de posições (AC: 6)
  - [ ] `backend/api/routes/positions.py`: `GET /api/v1/positions`
  - [ ] Buscar trades com `status="open"` do banco
  - [ ] Enriquecer com preço atual do cache Redis
  - [ ] Calcular P&L atual: `(current_price - entry_price) / entry_price * 100 * side_multiplier`
  - [ ] Retornar lista de `PositionSchema`
- [ ] Criar schemas Pydantic de posições (AC: 6)
  - [ ] `backend/api/schemas/trade.py`: `PositionSchema`, `TradeSchema`, `OpenPositionRequestSchema`
- [ ] Escrever testes unitários para OCO (AC: 7)
  - [ ] `backend/tests/unit/test_order_executor.py`
  - [ ] Testar construção do payload OCO para LONG e SHORT
  - [ ] Testar rejeição de ordem sem stop_loss
  - [ ] Testar validação de stop_loss acima da entrada em LONG (deve rejeitar)
  - [ ] Testar fallback OCO: mock que OCO lança exceção → verificar que stop_market é tentado
  - [ ] Testar fallback duplo: OCO falha + stop_market falha → verificar que fecha a mercado
  - [ ] Cobertura: 100% do `order_executor.py` (conforme PRD AC7)

## Dev Notes

**Binance OCO API (via CCXT):**
```python
# Envio de OCO (One-Cancels-Other)
order = await exchange.create_order(
    symbol="BTC/USDT",
    type="oco",
    side="sell",  # fechar posição LONG
    amount=0.001,
    params={
        "price": 294000,          # take profit (limit price)
        "stopPrice": 282900,      # stop loss trigger
        "stopLimitPrice": 282500, # stop limit price (um pouco abaixo do trigger)
        "stopLimitTimeInForce": "GTC"
    }
)
```

**Fallback de falha OCO:**
```python
async def _send_oco_with_fallback(self, trade: Trade, symbol, side, quantity, stop, tp):
    try:
        oco = await connector.place_oco_order(symbol, side, quantity, stop, tp)
        await trade_event_repo.create(trade_id=trade.id, event_type="OCO_SET", payload=oco)
    except Exception as e:
        log.error("oco_failed", error=str(e), trade_id=trade.id)
        try:
            # Fallback: stop-market separado
            stop_order = await connector.place_stop_market_order(symbol, opposite_side, quantity, stop)
            await trade_event_repo.create(trade_id=trade.id, event_type="OCO_FAILED",
                                           payload={"error": str(e), "fallback": "stop_market", "order": stop_order})
        except Exception as e2:
            # Fallback final: fechar a mercado
            log.critical("stop_market_failed_closing_at_market", trade_id=trade.id)
            await self.close_position_at_market(trade)
            await trade_event_repo.create(trade_id=trade.id, event_type="OCO_FAILED",
                                           payload={"error": str(e), "fallback": "market_close"})
        await telegram.send_emergency_alert(f"⚠️ OCO FAILED para {symbol} (trade #{trade.id})")
```

**Tipos de eventos em `trade_events`:**
```
SIGNAL_GENERATED, ORDER_SENT, ORDER_EXECUTED, OCO_SET, OCO_FAILED,
TP_HIT, SL_HIT, MANUAL_CLOSE, DD_LIMIT_CLOSE, TELEGRAM_SENT, ERROR
```

**Localização dos arquivos:**
```
backend/trading/order_executor.py
backend/trading/order_monitor.py
backend/api/routes/positions.py
backend/api/schemas/trade.py
backend/tests/unit/test_order_executor.py
```

**Referências:**
- `docs/fullstack-architecture.md` seção 2.1 (schema tabelas `trades` e `trade_events`)
- `docs/fullstack-architecture.md` seção 3.2 (endpoint GET /positions)
- `docs/fullstack-architecture.md` seção 4.1 (fluxo de trading — sequência de abertura)
- `docs/fullstack-architecture.md` seção 4.2 (fluxo de fechamento de posição)
- `docs/prd.md` Story 2.2 AC8-9 (fallback OCO e órfãos)

### Testing

**Framework:** pytest + pytest-asyncio + unittest.mock

**Localização:** `backend/tests/unit/test_order_executor.py`

**Cobertura obrigatória:** 90% em `backend/trading/order_executor.py` (conforme arquitetura seção 10.2 — "order_executor: 90%")

**Exemplo de teste de fallback:**
```python
@pytest.mark.asyncio
async def test_oco_failure_triggers_stop_market_fallback(mock_connector):
    mock_connector.place_oco_order.side_effect = Exception("Exchange rejected OCO")
    mock_connector.place_stop_market_order.return_value = {"orderId": "999"}

    executor = OrderExecutor(connector=mock_connector, ...)
    await executor._send_oco_with_fallback(trade, "BTC/USDT", "sell", 0.001, 282900, 294000)

    mock_connector.place_stop_market_order.assert_called_once()
    # Verificar que evento OCO_FAILED foi registrado
```

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 28/04/2026 | 1.0 | Criação inicial | SM (BMad) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## QA Results
